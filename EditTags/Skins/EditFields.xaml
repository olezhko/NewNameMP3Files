<UserControl x:Class="EditTags.Skins.EditFields"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="31" Width="220" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <TextBox Visibility="Hidden" Text="{Binding ChangedField}" Background="White" Foreground="Black"/>
        <Label Content="{Binding Text}" Background="White" Foreground="Black"/>
    </Grid>
</UserControl>
